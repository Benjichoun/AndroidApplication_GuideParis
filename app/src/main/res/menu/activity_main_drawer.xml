<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item android:id="@+id/nav_accueil" android:icon="@drawable/ic_menu_eiffel"
            android:title="Accueil" />
        <item android:id="@+id/nav_arrondissements" android:icon="@drawable/ic_menu_arrond"
            android:title="Arrondissements" />
        <item android:id="@+id/nav_calendrier" android:icon="@android:drawable/ic_menu_my_calendar"
            android:title="Calendrier" />
    </group>

    <item android:title="Contact">
        <menu>
            <item android:id="@+id/nav_partage" android:icon="@android:drawable/ic_menu_share"
                android:title="Partager" />
            <item android:id="@+id/nav_contact" android:icon="@android:drawable/ic_menu_send"
                android:title="Informations" />
        </menu>
    </item>

</menu>